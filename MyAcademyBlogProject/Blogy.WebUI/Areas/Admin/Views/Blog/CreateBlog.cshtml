@model CreateBlogDto
@{
    ViewData["Title"] = "Yeni Blog Oluştur";
    Layout = "~/Areas/Admin/Views/Admin/Index.cshtml";
}

<div class="container-fluid py-4">
    <div class="row">
        <div class="col-12">
            <div class="card my-4">
                <div class="card-header p-0 position-relative mt-n4 mx-3 z-index-2">
                    <div class="bg-gradient-danger shadow-dark border-radius-lg pt-4 pb-3">
                        <h6 class="text-white text-capitalize ps-3 mb-0">Yeni Blog Oluştur</h6>
                    </div>
                </div>

                <div class="card-body">
                    <form method="post">

                        <div class="input-group input-group-outline mb-4">
                            <label class="form-label">Blog Başlığı</label>
                            <input type="text" asp-for="Title" class="form-control" />
                        </div>
                        <span asp-validation-for="Title" class="text-danger text-xs"></span>

                        <div class="mb-4">
                            <label class="form-label text-dark font-weight-bold ms-1">Kategori Seçiniz</label>
                            <div class="input-group input-group-outline">
                                <select asp-for="CategoryId" asp-items="@ViewBag.categories" class="form-control form-select px-3">
                                    <option value="" selected disabled>Lütfen bir kategori seçin...</option>
                                </select>
                            </div>
                            <span asp-validation-for="CategoryId" class="text-danger text-xs"></span>
                        </div>

                        <div class="mb-4">
                            <label class="form-label text-dark font-weight-bold ms-1">Etiketler (Ctrl tuşuna basarak çoklu seçebilirsiniz)</label>
                            <div class="input-group input-group-outline">
                                <select name="TagIds" asp-items="@ViewBag.tags" class="form-control form-select px-3" multiple style="height: 100px;">
                                </select>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-4">
                                <div class="input-group input-group-outline mb-4">
                                    <label class="form-label">Kapak Resmi (Url)</label>
                                    <input type="text" asp-for="CoverImage" class="form-control" />
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="input-group input-group-outline mb-4">
                                    <label class="form-label">Blog Görsel 1 (Url)</label>
                                    <input type="text" asp-for="BlogImage1" class="form-control" />
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="input-group input-group-outline mb-4">
                                    <label class="form-label">Blog Görsel 2 (Url)</label>
                                    <input type="text" asp-for="BlogImage2" class="form-control" />
                                </div>
                            </div>
                        </div>
                        <div class="form-group mb-3">
                            <label>Blog İçeriği</label>

                            <button type="button" class="btn btn-info btn-sm mb-2 text-white" data-bs-toggle="modal" data-bs-target="#aiModal">
                                <i class="fa fa-magic"></i> AI ile Makale Yazdır
                            </button>

                            <textarea asp-for="Description" class="form-control" rows="10" id="BlogContent"></textarea>
                        </div>
                        <div class="input-group input-group-outline mb-4">
                            <label class="form-label">İçerik / Açıklama</label>
                            <textarea asp-for="Description" class="form-control" rows="8"></textarea>
                        </div>
                        <span asp-validation-for="Description" class="text-danger text-xs"></span>

                        <div class="d-flex justify-content-end">
                            <a href="/Admin/Blog/Index" class="btn btn-outline-secondary me-2">Vazgeç</a>
                            <button type="submit" class="btn bg-gradient-danger">Kaydet</button>
                        </div>

                    </form>
                    <div class="modal fade" id="aiModal" tabindex="-1" aria-labelledby="aiModalLabel" aria-hidden="true">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header bg-info text-white">
                                    <h5 class="modal-title" id="aiModalLabel">Yapay Zeka Asistanı 🤖</h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                </div>
                                <div class="modal-body">
                                    <div class="mb-3">
                                        <label class="form-label">Anahtar Kelimeler (Virgülle ayırın)</label>
                                        <input type="text" class="form-control" id="aiKeywords" placeholder="Örn: Teknoloji, Yazılım, C#">
                                    </div>
                                    <div class="mb-3">
                                        <label class="form-label">Konu Cümlesi</label>
                                        <textarea class="form-control" id="aiPrompt" rows="3" placeholder="Örn: Yazılım dünyasındaki son gelişmeler hakkında bilgi ver."></textarea>
                                    </div>
                                    <div id="loadingArea" style="display:none;" class="text-center text-info">
                                        <div class="spinner-border" role="status"></div>
                                        <p>Makale yazılıyor, lütfen bekleyin...</p>
                                    </div>
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Vazgeç</button>
                                    <button type="button" class="btn btn-primary" onclick="generateArticle()">Oluştur ve Aktar</button>
                                </div>
                            </div>
                        </div>
                    </div>

                    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
                    <script>
                        function generateArticle() {
                            var keywords = document.getElementById("aiKeywords").value;
                            var prompt = document.getElementById("aiPrompt").value;

                            if (keywords === "" || prompt === "") {
                                alert("Lütfen tüm alanları doldurun!");
                                return;
                            }

                            // Yükleniyor animasyonunu aç
                            document.getElementById("loadingArea").style.display = "block";

                            var data = {
                                keywords: keywords,
                                prompt: prompt
                            };

                            $.ajax({
                                type: "POST",
                                url: "/Admin/Blog/GenerateAiArticle", // Controller Adresi
                                data: JSON.stringify(data),
                                contentType: "application/json; charset=utf-8",
                                dataType: "json",
                                success: function (response) {
                                    // Gelen makaleyi textarea'ya yaz
                                    document.getElementById("BlogContent").value = response.content;

                                    // Modalı kapat ve yükleniyor gizle
                                    document.getElementById("loadingArea").style.display = "none";
                                    var modalElement = document.getElementById('aiModal');
                                    var modal = bootstrap.Modal.getInstance(modalElement);
                                    modal.hide();

                                    alert("Makale başarıyla oluşturuldu! 🚀");
                                },
                                error: function (err) {
                                    document.getElementById("loadingArea").style.display = "none";
                                    alert("Bir hata oluştu. Lütfen tekrar deneyin.");
                                    console.log(err);
                                }
                            });
                        }
                    </script>
                </div>
            </div>
        </div>
    </div>
</div>
